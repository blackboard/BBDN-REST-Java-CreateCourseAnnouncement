BBDN-REST-Java-CreateCourseAnnouncement
==========
To build, simply run ``gradle build`` at the commandline. To build documentation, run ``gradle javadoc`` at the commandline. To run tests, run ``gradle test`` at the commandline.
